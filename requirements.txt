# Core dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
scikit-learn>=1.3.0

# Training
trl>=0.7.0
datasets>=2.14.0

# RL & Optimization
numpy>=1.24.0
scipy>=1.10.0

# Data processing
pandas>=2.0.0
pyarrow>=12.0.0

# Database (sqlite3 comes with Python standard library)

# Utilities
pyyaml>=6.0
timeout-decorator>=0.5.0
tqdm>=4.65.0

# Evaluation
scikit-learn>=1.3.0

# Optional: Faster training (Linux/CUDA only - recommended for Kaggle)
# For Kaggle notebooks, install with:
# !pip install unsloth[kaggle] @ git+https://github.com/unslothai/unsloth.git
unsloth @ git+https://github.com/unslothai/unsloth.git  # Will install only on compatible systems

# Optional: Better logging
wandb>=0.15.0  # For experiment tracking (optional)
tensorboard>=2.13.0  # For logging (optional)

# Development
jupyter>=1.0.0
notebook>=7.0.0
ipywidgets>=8.0.0
